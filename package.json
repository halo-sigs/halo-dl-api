{
  "name": "halo-dl-api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@octokit/rest": "^22.0.1",
    "hono": "^4.11.7"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
